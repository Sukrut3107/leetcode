class Solution {
    public int reverse(int n){
        int result = 0;
        int prev = 0;
        while(n!=0){
            int cur = n%10;
            n = n/10;
            result = result*10+cur;

            if((result - cur)/10!=prev) return 0;
            prev = result;
        }
        return result;
}
}
